name: Render architecture diagram

on:
  push:
    paths:
      - 'images/architecture.mmd'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  render:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Render SVG from Mermaid source
        run: npx -p @mermaid-js/mermaid-cli mmdc -i images/architecture.mmd -o images/image3-light.svg -b transparent -p images/puppeteer-config.json

      - name: Commit updated SVG
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add images/image-light.svg
          git diff --staged --quiet || git commit -m "Auto-render architecture diagram"
          git push
